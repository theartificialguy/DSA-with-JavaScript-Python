/*

=> QUEUE -> a data structure based on FIFO principle (first-in-first-out)
=> Operations -> enqueue, dequeue, peek (front), isEmpty, size.
=> Insertion (enqueue) & Deletion (dequeue) happens from the opposite ends.
	-> enqueue from end (rear)
	-> dequeue from start (front)
	-> front (top) - queue[0]
=> Typical questions:
	
*/